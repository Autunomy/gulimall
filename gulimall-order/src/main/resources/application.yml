server:
  port: 9000

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://10.10.10.163:3306/gulimall_oms
    driver-class-name: com.mysql.jdbc.Driver
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto # id自增
